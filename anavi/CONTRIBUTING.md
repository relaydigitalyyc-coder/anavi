# Contributing

## Running Tests

```bash
pnpm test
```

Includes unit tests and integration tests (core funnel: intent → match → deal room). Integration tests use mocks and run without a database.

## Type Check & Build

```bash
pnpm check   # TypeScript
pnpm build   # Client + server
```
